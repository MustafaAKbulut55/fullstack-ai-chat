# ============================================================
# 🌍 Environment Variables (.env)
# Bu dosya, uygulamanın ortam (environment) değişkenlerini içerir.
# Buradaki değerler frontend (React / Vite) tarafında kullanılır.
#
# NOT:
#  - Vite projelerinde değişkenlerin "VITE_" ile başlaması zorunludur.
#  - .env dosyası genellikle sürüm kontrolüne (git) dahil edilmez.
# ============================================================

# 🔹 Backend API adresi (Render üzerinde çalışan .NET API)
# Bu URL, ChatScreen veya diğer servislerin fetch() isteklerinde kullanılır.
VITE_API_BASE_URL=https://fullstack-ai-chat-dpog.onrender.com
